const tableName = '';

exports.up = ({ schema }) => {
  return schema.createTable(tableName, (table) => {
    table.increments('id');
    
    table.timestamps();
  });
};

exports.down = ({ schema }) => {
  return schema.dropTable(tableName);
};
